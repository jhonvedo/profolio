---
import type { Lang } from "./i18n";
const { lang } = Astro.props as { lang: Lang };
const target = lang === "es" ? "en" : "es";
---

<a href={`/${target}`} class="rounded-xl border border-white/10 px-3 py-2 text-xs font-semibold">{target.toUpperCase()}</a>
